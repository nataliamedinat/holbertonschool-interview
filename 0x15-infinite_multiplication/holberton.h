#ifndef INFINITE_MUL_
#define INFINITE_MUL_

/** Libraries */
#include <stdio.h>
#include <stdlib.h>
#include <errno.h>

/** Prototypes */
int _putchar(char c);
int isInt(char *c);
int _strlen(char *c);
void *_calloc(unsigned int nmemb, unsigned int size);
void inf_mult(char *num1, char *num2);

#endif
